name: CI

on:
  push:
  pull_request:
  schedule:
    - cron: '0 0 * * *'

jobs:
  fmt-inside-folder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout V
        uses: actions/checkout@v2
        with:
          repository: vlang/v
      - name: Build V
        run: make
      - name: Checkout termtable
        uses: actions/checkout@v2
        with:
            path: termtable
      - name: v fmt -verify
        run: |
          cd termtable
          ../v fmt -diff .
          ../v fmt -verify .

  fmt-outside-folder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout V
        uses: actions/checkout@v2
        with:
          repository: vlang/v
      - name: Build V
        run: make
      - name: Checkout termtable
        uses: actions/checkout@v2
        with:
            path: termtable
      - name: v fmt -verify
        run: |
          ./v fmt -diff termtable
          ./v fmt -verify termtable

  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout V
  #       uses: actions/checkout@v2
  #       with:
  #         repository: vlang/v
  #     - name: Build V
  #       run: make
  #     - name: Checkout termtable
  #       uses: actions/checkout@v2
  #       with:
  #           path: termtable
  #     - name: Run tests
  #       run: ./v -W test termtable

  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout V
  #       uses: actions/checkout@v2
  #       with:
  #         repository: vlang/v
  #     - name: Build V
  #       run: make
  #     - name: Checkout termtable
  #       uses: actions/checkout@v2
  #       with:
  #           path: termtable
  #     - name: Build termtable
  #       run: ./v -prod -shared termtable
